//\! TDB Stats tool

use super::ToolResult;
use std::path::PathBuf;

pub async fn run(_location: PathBuf, _detailed: bool, _format: String) -> ToolResult {
    println!("TDB Stats tool not yet implemented");
    Ok(())
}
