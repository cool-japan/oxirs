//! Language tag validation tool

use super::ToolResult;

pub async fn run(_tag: String, _validate: bool, _normalize: bool) -> ToolResult {
    println!("Language tag validation tool not yet implemented");
    Ok(())
}
