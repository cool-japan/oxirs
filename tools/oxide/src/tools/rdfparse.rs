//! RDF Parse tool

use super::ToolResult;
use std::path::PathBuf;

pub async fn run(file: PathBuf, format: Option<String>, base: Option<String>) -> ToolResult {
    println!("RDF Parse tool not yet implemented");
    Ok(())
}
